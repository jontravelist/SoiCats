// Explicit entry point for the app. We import expo-router/entry directly
// here instead of relying on package.json's "main" field, which Expo's CLI
// occasionally fails to resolve when running inside a npm workspace.

import "expo-router/entry";
